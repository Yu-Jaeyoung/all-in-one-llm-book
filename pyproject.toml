[project]
name = "all-in-one-llm-book"
version = "0.1.0"
description = "All in one LLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.6.1",
    "ipykernel>=7.2.0",
    "torch>=2.10.0",
]
